<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/AppActivityAnim</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppTheme.SwipeBack.NoActionBar" parent="AppTheme.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="AppTheme"/>

    <style name="AppActivityAnim">
        // 用于设置打开新的Activity并进入新的Activity展示的动画
        <item name="android:activityOpenEnterAnimation">@android:anim/slide_in_left</item>
        // 用于设置打开新的Activity并销毁之前的Activity展示的动画
        <item name="android:activityOpenExitAnimation">@android:anim/slide_out_right</item>
        // 用于设置关闭当前Activity进入上一个Activity展示的动画
        <item name="android:activityCloseEnterAnimation">@android:anim/slide_in_left</item>
        // 用于设置关闭当前Activity时展示的动画
        <item name="android:activityCloseExitAnimation">@android:anim/slide_out_right</item>
    </style>

</resources>
